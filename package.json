{
  "name": "furnishops",
  "version": "1.0.0",
  "private": true,
  "description": "Digital Order & Operations Management System - monorepo runner",
  "scripts": {
    "predev": "npm --prefix backend run db:prepare",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "seed": "npm --prefix backend run seed",
    "migrate": "npm --prefix backend run migrate",
    "studio": "npm --prefix backend run studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}